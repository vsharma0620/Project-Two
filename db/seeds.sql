-- USE open_mic_db;

-- CREATE TABLE Users( 
      
-- username varchar(100) NOT NULL,
-- presenting BOOLEAN NOT NULL,
-- category varchar(100) NOT NULL,
-- status INTEGER DEFAULT false,
-- hasPresented BOOLEAN DEFAULT false

-- )

-- USE open_mic_db;

-- INSERT INTO Users(username, presenting, category, status, hasPresented )
-- VALUES
-- ("VS", 0, "Comedy", 0, 0),
-- ("LC", 0, "Comedy", 0, 0),
-- ("MR", 0, "Comedy", 0, 0),
-- ("BW", 0, "Comedy", 0, 0),
-- ("AL", 0, "Comedy", 0, 0);


